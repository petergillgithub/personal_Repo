// ES6 Module = An external file that contains reusable code that can be improved into oher javascript files.
// Write reusable code for many different apps..
// can contain variables, classes,functions ... and more Introduced as part of ECMAScript 2015 update...

import {add,subtract} from './math.js';

console.log(add(2,10));
console.log(subtract(10,6));